const express = require('express'); const axios = require('axios'); const app = express(); require('dotenv').config(); app.use(express.json()); app.get('/weather', async (req, res) => { const { city, unit } = req.query; try { const response = await axios.get(`https://api.weatherapi.com/v1/current.json?key=${process.env.WEATHER_API_KEY}&q=${city}&aqi=no`); const weatherData = { temperature: unit === 'celsius' ? response.data.current.temp_c : response.data.current.temp_f, rain: response.data.current.precip_mm, humidity: response.data.current.humidity }; res.json(weatherData); } catch (error) { console.error(error); res.status(500).json({ error: 'Internal Server Error' }); } }); const PORT = process.env.PORT || 3000; app.listen(PORT, () => { console.log(`Server is running on port ${PORT}`); });